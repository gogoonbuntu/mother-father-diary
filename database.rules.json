{
    "rules": {
        "users": {
            "$uid": {
                ".read": "$uid === auth.uid",
                ".write": "$uid === auth.uid"
            }
        },
        "model_ratings": {
            "$modelKey": {
                ".read": "auth != null",
                ".write": "auth != null"
            }
        },
        ".read": false,
        ".write": false
    }
}